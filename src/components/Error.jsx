const Error = ({children}) => {
    return (
        <div className="bg-red-500 text-cyan-50 text-center p-3 uppercase font-bold mb-3 rounded-md" >
            {children}
        </div>      
    )
}

export default Error
